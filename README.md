# expressjs-authentication-api
this is created by expressjs framework

database used in this project is mongodb.

clone the repo.

npm install
npm run start

run the following command in your command line and run the api. 
